apiVersion: radix.equinor.com/v1
kind: RadixApplication
metadata:
  name: fusion-app-ipad
spec:
  environments:
    - name: dev
      build:
        from: main
    - name: prod
      build:
        from: prod

  components:
    - name: frontend
      src: frontend
      ports:
        - name: http
          port: 3000
      publicPort: http
      environmentConfig:
        - environment: dev
          resources:
            requests:
              memory: "300Mi"
              cpu: "50m"
            limits:
              memory: "3000Mi"
              cpu: "1000m"
          runAsNonRoot: true
